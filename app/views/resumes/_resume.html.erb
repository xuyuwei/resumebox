<li id="resume-<%= resume.id %>">

	<span class="url"> <%= resume.url %></span>
	<span class="timestamp"> Posted <%= time_ago_in_words(resume.created_at) %> ago. </span>
	<%= link_to "delete", resume, method: :delete %>
</li>